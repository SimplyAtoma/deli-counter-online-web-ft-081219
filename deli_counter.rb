# Write your code here.
def line(order)
  index = 1
  if order[0] == nil
    puts "The line is currently empty."
  else
    order.each{ |name| 
        puts " #{index}. #{name}"
        index += 1}
  end
end 

def take_a_number
  
end 

def now_serving(order, name)
  index = 1
  if order[0] == nil
    puts "The line is currently empty."
  else
    order.each do 
      if order
  end
end 